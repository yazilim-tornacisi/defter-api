DATABASE_URL=postgres://postgres:change-me@localhost:5432/notes
PORT=4000
HOST=0.0.0.0
# Üretimde uzun, rastgele bir değer kullanın (örn: openssl rand -hex 32)
AUTH_SECRET=change-me-to-a-long-random-string
# İleride metrik önbelleği vb. için (şu an kullanılmıyor)
# REDIS_HOST=host:port
# REDIS_PASS=secret
